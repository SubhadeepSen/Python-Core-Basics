#Every package directory should have __init__.py file. The python interpreter identify a directory as package if it has __init__.py file inside.

def one():
    print("This is inside main-package: packageOne");
